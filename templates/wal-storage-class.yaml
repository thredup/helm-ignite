{{- if (.Values.wal_pvc.enabled) }}
kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: {{ include "ignite.fullname" . }}-wal-storage-class
provisioner: {{ .Values.wal_pvc.provisioner }}
parameters:
{{ toYaml .Values.wal_pvc.provisioner_parameters | indent 2 }}
{{- end }}
